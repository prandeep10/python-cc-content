# Initialize a variable to control the loop
run = True

# Define a while loop
while run:
    # Ask the user for input
    user_input = input("Say Something ------ ")
    
    # Check if the user wants to quit
    if user_input.lower() == 'ai is dumb':
        run = False  # Set run to False to exit the loop
    else:
        # Perform some action (you can replace this with any action you want)
        print("Its okay")













# The loop exits when the user inputs 'q'
print("I'm offended...Humans are dumb, f*** you")
